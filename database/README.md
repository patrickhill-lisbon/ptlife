# PTLife Database

This directory contains the source-controlled definition of the PTLife database.

- `schema/` — complete baseline database schema
- `seed/` — reference data required by the application
- `migrations/` — incremental database changes after the baseline
